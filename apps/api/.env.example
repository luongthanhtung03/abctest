DATABASE_URL=postgresql://abc:abc@localhost:5433/abc_quote
PORT=4000
WEB_ORIGIN=http://localhost:3000
JWT_SECRET=change-me-to-a-long-random-string
# Shared secret the Mac mini agent sends in X-Agent-Key
AGENT_API_KEY=dev-agent-key-change-me
# Template version pinned into each new quote's snapshot
CURRENT_TEMPLATE_VERSION=v1
STORAGE_DRIVER=local
STORAGE_DIR=./storage
